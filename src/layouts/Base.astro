---
import '../styles/global.css';
import Dino from '../components/Dino.astro';
import SiteSchema from '../components/SiteSchema.astro';

interface Props {
  title: string;
  description?: string;
}

const DEFAULT_DESCRIPTION =
  'Essays and conversations across time. By Adam Pang.';
const { title, description = DEFAULT_DESCRIPTION } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const path = Astro.url.pathname;
const isCurrent = (prefix: string) => path === prefix || path.startsWith(prefix + '/');
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="author" content="Adam Pang" />
    <link rel="canonical" href={canonicalURL} />
    <link rel="author" href="https://adampang.com/" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,300..900;1,9..144,300..700&family=Newsreader:ital,opsz,wght@0,6..72,300..600;1,6..72,300..500&display=swap"
    />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:site_name" content="Pangaea" />
    <meta name="twitter:card" content="summary" />
    <link rel="alternate" type="application/rss+xml" title="Pangaea Essays" href="/rss.xml" />
    <link rel="alternate" type="application/rss+xml" title="Pangaea Pod" href="/podcast.xml" />
    <SiteSchema />
  </head>
  <body>
    <header class="site">
      <a class="brand" href="/">Pangaea<span class="dot">.</span></a>
      <nav aria-label="Primary">
        <a href="/posts/" aria-current={isCurrent('/posts') ? 'page' : undefined}>Read</a>
        <a href="/podcast/" aria-current={isCurrent('/podcast') ? 'page' : undefined}>Listen</a>
        <a href="/about/" aria-current={isCurrent('/about') ? 'page' : undefined}>About</a>
      </nav>
    </header>
    <main>
      <slot />
    </main>
    <footer class="site">
      <span class="footer-mark">
        <Dino height={20} />
        Pangaea. Essays and conversations across time.
      </span>
      <span>
        <a href="/archive/">Archive</a> · <a href="/graph/">Graph</a> ·
        <a href="/contact/">Contact</a> · <a href="/privacy/">Privacy</a> ·
        <a href="/rss.xml">Essay RSS</a> · <a href="/podcast.xml">Podcast RSS</a>
      </span>
    </footer>
  </body>
</html>
