---
interface Props {
  attribution?: string;
}

const { attribution } = Astro.props;
---

<blockquote class="pullquote">
  <slot />
  {attribution && <cite class="attribution">{attribution}</cite>}
</blockquote>
